# M3 实现计划:Skills 按需加载

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 消息命中技能触发词时,把技能 prompt 注入本轮 systemPrompt(身份之后、记忆之前);不命中不注入。

**Architecture:** `Skill` 类型 + `matchSkills` 纯函数(shared);`SkillStore` 端口 + `FileSkillStore`(读取 skills/manifest.json + prompts/*.md)+ 内存实现(api);`buildSystemPrompt` 增加 skills 段;executeTurn 每轮匹配注入;`GET /api/skills` 只读列表。

**Tech Stack:** 同 M1/M2(TS / Fastify / vitest)。新增 skills 目录(manifest.json + 3 个 md)。

## Global Constraints

- 沿用 M1/M2 全部约束(ESM `.js` 后缀、端口-适配器、提交规范)
- 注入顺序固定:**身份 → 技能 → 团队记忆**
- 匹配为大小写不敏感的包含匹配(任一触发词命中即算)
- 新增用例 ≥ 12

---

### Task 1: Skill 类型 + matchSkills(shared 纯函数 TDD)

**Files:**
- Modify: `packages/shared/src/types.ts`(追加 Skill)
- Create: `packages/shared/src/skills.ts`
- Create: `packages/shared/test/skills.test.ts`
- Modify: `packages/shared/src/index.ts`

**Interfaces:**
- Produces: `Skill` 类型;`matchSkills(content: string, skills: Skill[]): Skill[]`

- [ ] **Step 1: 写失败测试**

`packages/shared/test/skills.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { matchSkills } from '../src/skills.js';
import type { Skill } from '../src/types.js';

const skills: Skill[] = [
  { id: 'tdd', name: '测试驱动开发', description: 'd', triggers: ['tdd', '测试驱动'], prompt: '红绿重构' },
  { id: 'review', name: '代码审查', description: 'd', triggers: ['review', '审查'], prompt: '审查清单' },
];

describe('matchSkills', () => {
  it('命中英文触发词(大小写不敏感)', () => {
    expect(matchSkills('帮我 REVIEW 一下', skills).map((s) => s.id)).toEqual(['review']);
  });

  it('命中中文触发词', () => {
    expect(matchSkills('用测试驱动来写', skills).map((s) => s.id)).toEqual(['tdd']);
  });

  it('多技能同时命中', () => {
    expect(matchSkills('先 tdd 再 review', skills).map((s) => s.id)).toEqual(['tdd', 'review']);
  });

  it('无命中返回空数组', () => {
    expect(matchSkills('今天天气不错', skills)).toEqual([]);
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `cd /Users/xuzishuo/code/meowbase && pnpm --filter @meowbase/shared exec vitest run test/skills.test.ts`
Expected: FAIL,模块不存在。

- [ ] **Step 3: 实现**

`packages/shared/src/types.ts`(追加):

```ts
export interface Skill {
  id: string;
  name: string;
  description: string;
  triggers: string[];
  prompt: string;
}
```

`packages/shared/src/skills.ts`:

```ts
import type { Skill } from './types.js';

export function matchSkills(content: string, skills: Skill[]): Skill[] {
  const lower = content.toLowerCase();
  return skills.filter((skill) =>
    skill.triggers.some((trigger) => lower.includes(trigger.toLowerCase())),
  );
}
```

`packages/shared/src/index.ts`(追加):

```ts
export type { Skill } from './types.js';
export { matchSkills } from './skills.js';
```

- [ ] **Step 4: 运行确认通过**

Run: 同上命令
Expected: 4 个用例全部 PASS。

- [ ] **Step 5: 提交**

```bash
git add -A && git commit -m "feat(shared): Skill 类型 + matchSkills 匹配"
```

---

### Task 2: buildSystemPrompt 扩展技能段(shared TDD)

**Files:**
- Modify: `packages/shared/src/system-prompt.ts`
- Modify: `packages/shared/test/system-prompt.test.ts`

**Interfaces:**
- Consumes: `Skill`(Task 1)
- Produces: `buildSystemPrompt` input 增加 `skills?: Skill[]`;段落顺序 身份 → 技能 → 团队记忆

- [ ] **Step 1: 写失败测试(追加)**

`packages/shared/test/system-prompt.test.ts`(追加):

```ts
import type { Skill } from '../src/types.js';

const skill: Skill = {
  id: 'tdd',
  name: '测试驱动开发',
  description: 'd',
  triggers: ['tdd'],
  prompt: '红-绿-重构循环',
};

describe('buildSystemPrompt 技能段', () => {
  it('命中技能时注入技能段,位于身份之后记忆之前', () => {
    const prompt = buildSystemPrompt({ profile, skills: [skill], evidenceRefs: [evidence] });
    expect(prompt).toContain('[技能:测试驱动开发] 红-绿-重构循环');
    const idxIdentity = prompt?.indexOf('你是 墨墨') ?? -1;
    const idxSkill = prompt?.indexOf('[技能:测试驱动开发]') ?? -1;
    const idxMemory = prompt?.indexOf('团队记忆') ?? -1;
    expect(idxSkill).toBeGreaterThan(idxIdentity);
    expect(idxMemory).toBeGreaterThan(idxSkill);
  });

  it('无技能时不输出技能段', () => {
    const prompt = buildSystemPrompt({ profile, skills: [], evidenceRefs: [] });
    expect(prompt).not.toContain('[技能:');
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @meowbase/shared exec vitest run test/system-prompt.test.ts`
Expected: 新 2 个用例 FAIL(无 skills 字段支持)。

- [ ] **Step 3: 实现**

`packages/shared/src/system-prompt.ts`:

```ts
import type { AgentProfile, EvidenceEntry, Skill } from './types.js';

export function buildSystemPrompt(input: {
  profile?: AgentProfile;
  skills?: Skill[];
  evidenceRefs: EvidenceEntry[];
}): string | undefined {
  const parts: string[] = [];
  if (input.profile) {
    const p = input.profile;
    parts.push(
      `你是 ${p.name},${p.role}。性格:${p.personality}。擅长:${p.expertise.join('、')}。`,
    );
  }
  if (input.skills && input.skills.length > 0) {
    const lines = input.skills.map((s) => `[技能:${s.name}] ${s.prompt}`);
    parts.push(`本轮请遵循以下技能:\n${lines.join('\n\n')}`);
  }
  if (input.evidenceRefs.length > 0) {
    const lines = input.evidenceRefs.map(
      (e) => `- [${e.kind}] ${e.title}: ${e.content}`,
    );
    parts.push(`团队记忆:\n${lines.join('\n')}`);
  }
  return parts.length > 0 ? parts.join('\n\n') : undefined;
}
```

- [ ] **Step 4: 运行确认通过**

Run: 同上命令
Expected: 原有 4 个 + 新增 2 个全部 PASS。

- [ ] **Step 5: 提交**

```bash
git add -A && git commit -m "feat(shared): buildSystemPrompt 技能段"
```

---

### Task 3: SkillStore 端口 + 内存/文件实现 + 内置技能

**Files:**
- Create: `skills/manifest.json`
- Create: `skills/prompts/tdd.md`
- Create: `skills/prompts/review.md`
- Create: `skills/prompts/debug.md`
- Modify: `packages/api/src/stores/ports.ts`(SkillStore)
- Modify: `packages/api/src/stores/memory.ts`(InMemorySkillStore)
- Create: `packages/api/src/stores/file-skill-store.ts`
- Modify: `packages/api/src/stores/factories.ts`(createMemoryStores 加 skills;createSkillStore)
- Modify: `packages/api/src/config.ts`(skillsDir)
- Create: `packages/api/test/file-skill-store.test.ts`
- Create: `packages/api/test/fixtures/skills-fixture/manifest.json`
- Create: `packages/api/test/fixtures/skills-fixture/prompts/tdd.md`

**Interfaces:**
- Produces: `SkillStore { list(): Promise<Skill[]>; get(id): Promise<Skill | null> }`;`FileSkillStore(dir)`;`createSkillStore(skillsDir)`;`createMemoryStores()` 增加 `skills`

- [ ] **Step 1: 写失败测试**

`packages/api/test/fixtures/skills-fixture/manifest.json`:

```json
{
  "skills": [
    {
      "id": "tdd",
      "name": "测试驱动开发",
      "description": "先写失败测试再实现",
      "triggers": ["tdd", "测试驱动"],
      "promptFile": "tdd.md"
    }
  ]
}
```

`packages/api/test/fixtures/skills-fixture/prompts/tdd.md`:

```markdown
红-绿-重构循环:先写失败测试,再写最小实现,最后重构。
```

`packages/api/test/file-skill-store.test.ts`:

```ts
import { join } from 'node:path';
import { describe, expect, it } from 'vitest';
import { FileSkillStore } from '../src/stores/file-skill-store.js';

const FIXTURE_DIR = join(import.meta.dirname, 'fixtures', 'skills-fixture');

describe('FileSkillStore', () => {
  it('加载 manifest 与 prompt 文件', async () => {
    const store = new FileSkillStore(FIXTURE_DIR);
    const list = await store.list();
    expect(list.length).toBe(1);
    expect(list[0]?.id).toBe('tdd');
    expect(list[0]?.prompt).toContain('红-绿-重构');
    expect((await store.get('tdd'))?.triggers).toEqual(['tdd', '测试驱动']);
    expect(await store.get('不存在')).toBeNull();
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `cd /Users/xuzishuo/code/meowbase && pnpm --filter @meowbase/api exec vitest run test/file-skill-store.test.ts`
Expected: FAIL,模块不存在。

- [ ] **Step 3: 实现**

`packages/api/src/stores/ports.ts`(追加):

```ts
import type { Skill } from '@meowbase/shared';

export interface SkillStore {
  list(): Promise<Skill[]>;
  get(id: string): Promise<Skill | null>;
}
```

`packages/api/src/stores/memory.ts`(追加):

```ts
import type { Skill } from '@meowbase/shared';
import type { SkillStore } from './ports.js';

export class InMemorySkillStore implements SkillStore {
  private readonly skills = new Map<string, Skill>();

  constructor(skills: Skill[] = []) {
    for (const skill of skills) this.skills.set(skill.id, skill);
  }

  async list(): Promise<Skill[]> {
    return [...this.skills.values()];
  }

  async get(id: string): Promise<Skill | null> {
    return this.skills.get(id) ?? null;
  }
}
```

`packages/api/src/stores/file-skill-store.ts`:

```ts
import { readFileSync } from 'node:fs';
import { join } from 'node:path';
import type { Skill } from '@meowbase/shared';
import type { SkillStore } from './ports.js';

interface ManifestSkill {
  id: string;
  name: string;
  description: string;
  triggers: string[];
  promptFile: string;
}

export class FileSkillStore implements SkillStore {
  private readonly skills = new Map<string, Skill>();

  constructor(private readonly rootDir: string) {
    this.load();
  }

  private load(): void {
    const manifest = JSON.parse(
      readFileSync(join(this.rootDir, 'manifest.json'), 'utf8'),
    ) as { skills: ManifestSkill[] };
    for (const item of manifest.skills) {
      const prompt = readFileSync(
        join(this.rootDir, 'prompts', item.promptFile),
        'utf8',
      ).trim();
      this.skills.set(item.id, {
        id: item.id,
        name: item.name,
        description: item.description,
        triggers: item.triggers,
        prompt,
      });
    }
  }

  async list(): Promise<Skill[]> {
    return [...this.skills.values()];
  }

  async get(id: string): Promise<Skill | null> {
    return this.skills.get(id) ?? null;
  }
}
```

`packages/api/src/stores/factories.ts`(追加):

```ts
import { FileSkillStore } from './file-skill-store.js';
import { InMemorySkillStore } from './memory.js';
import type { SkillStore } from './ports.js';

// createMemoryStores 返回对象追加:
//   skills: new InMemorySkillStore(),

export function createSkillStore(skillsDir: string): SkillStore {
  return new FileSkillStore(skillsDir);
}
```

`packages/api/src/config.ts`(追加字段):

```ts
skillsDir: string; // = env.SKILLS_DIR ?? './skills'
```

- [ ] **Step 4: 运行确认通过**

Run: 同上命令
Expected: 1 个用例 PASS。

- [ ] **Step 5: 内置技能文件(仓库根 skills/)**

`skills/manifest.json`:

```json
{
  "skills": [
    {
      "id": "tdd",
      "name": "测试驱动开发",
      "description": "先写失败测试,再实现,再重构",
      "triggers": ["tdd", "测试驱动"],
      "promptFile": "tdd.md"
    },
    {
      "id": "review",
      "name": "代码审查",
      "description": "按清单系统化审查代码",
      "triggers": ["review", "审查", "代码评审"],
      "promptFile": "review.md"
    },
    {
      "id": "debug",
      "name": "系统化调试",
      "description": "按流程定位并修复问题",
      "triggers": ["debug", "调试", "bug"],
      "promptFile": "debug.md"
    }
  ]
}
```

`skills/prompts/tdd.md`:

```markdown
请遵循测试驱动开发:
1. 先写一个会失败的测试,明确预期行为
2. 运行确认它失败(红)
3. 写最小实现让测试通过(绿)
4. 重构,保持测试全绿
不要跳过测试直接写实现。
```

`skills/prompts/review.md`:

```markdown
请按清单审查代码:
1. 正确性:逻辑是否成立,边界条件是否覆盖
2. 安全:输入校验、注入、敏感信息
3. 可读性:命名、结构、注释是否恰当
4. 测试:是否有对应测试,覆盖关键路径
输出格式:问题列表(严重程度)→ 建议 → 结论(通过/需修改)
```

`skills/prompts/debug.md`:

```markdown
请系统化调试:
1. 先复现问题,记录稳定复现步骤
2. 读完整错误信息,定位最近改动
3. 提出假设,用最小实验验证(二分定位)
4. 修复后运行相关测试,确认无回归
不要在没有复现的情况下猜测修改。
```

- [ ] **Step 6: 提交**

```bash
git add -A && git commit -m "feat(api): SkillStore(文件加载)+ 内置三个技能"
```

---

### Task 4: executeTurn 技能注入(api TDD)

**Files:**
- Modify: `packages/api/src/router/execute-turn.ts`
- Modify: `packages/api/test/execute-turn.test.ts`(追加用例)

**Interfaces:**
- Consumes: `matchSkills`(Task 1)、`SkillStore`(Task 3)
- Produces: `TurnContext.stores` 增加 `skills: SkillStore`;每轮命中技能注入 systemPrompt

- [ ] **Step 1: 写失败测试(追加)**

`packages/api/test/execute-turn.test.ts`(追加):

```ts
describe('executeTurn 技能注入', () => {
  it('命中触发词 → systemPrompt 含技能;不命中 → 不含', async () => {
    const stores = createMemoryStores();
    const prompts: (string | undefined)[] = [];
    const registry = createAgentRegistry([
      {
        agentId: 'claude',
        async runTurn(input) {
          prompts.push(input.systemPrompt);
          return { sessionId: 's1', content: 'ok', status: 'completed' };
        },
      },
    ]);
    await stores.skills.list; // noop,确保字段存在
    const thread = await stores.threads.create({ title: 't', primaryAgentId: 'claude' });

    // 第一轮:命中 review 触发词,新会话同时注入身份
    await executeTurn({
      threadId: thread.id, content: '帮我 review 这段代码', context: { stores, registry },
    });
    expect(prompts[0]).toContain('[技能:代码审查]');

    // 第二轮:无触发词,resume 不注入身份,也不注入技能
    await executeTurn({ threadId: thread.id, content: '继续', context: { stores, registry } });
    expect(prompts[1]).toBeUndefined();
  });
});
```

注意:该测试依赖 `createMemoryStores()` 提供 `skills`(Task 3 已加,空列表)。要让它有意义,需先给内存 store 注入技能——修改工厂或在测试里构造:

```ts
// 若 createMemoryStores 的 skills 为空,上面第一轮断言会失败(无技能可匹配)
// 正确做法:createMemoryStores 追加一个可选参数:
// createMemoryStores(skills: Skill[] = [])
```

- [ ] **Step 2: 运行确认失败**

Run: `cd /Users/xuzishuo/code/meowbase && pnpm --filter @meowbase/api exec vitest run test/execute-turn.test.ts`
Expected: 新用例 FAIL。

- [ ] **Step 3: 实现**

`packages/api/src/stores/factories.ts`(createMemoryStores 支持注入技能):

```ts
import type { Skill } from '@meowbase/shared';

export function createMemoryStores(skills: Skill[] = []): {
  threads: ThreadStore;
  messages: MessageStore;
  profiles: ProfileStore;
  evidence: EvidenceStore;
  skills: SkillStore;
} {
  return {
    threads: new InMemoryThreadStore(),
    messages: new InMemoryMessageStore(),
    profiles: new InMemoryProfileStore(),
    evidence: new InMemoryEvidenceStore(),
    skills: new InMemorySkillStore(skills),
  };
}
```

`packages/api/src/router/execute-turn.ts`(改动):

```ts
import { buildSystemPrompt, matchSkills, parseConfirmCommand, parseEvidenceRefs, parseLearnCommand, resolveTargetAgent } from '@meowbase/shared';
import type { AgentId, EvidenceEntry, Message, Skill } from '@meowbase/shared';
import type { EvidenceStore, MessageStore, ProfileStore, SkillStore, ThreadStore } from '../stores/ports.js';

export interface TurnContext {
  stores: {
    threads: ThreadStore;
    messages: MessageStore;
    profiles: ProfileStore;
    evidence: EvidenceStore;
    skills: SkillStore;
  };
  registry: AgentRegistry;
  onIncrement?: (threadId: string, messageId: string, delta: string) => void;
}
```

在 systemPrompt 组装处:

```ts
  const matchedSkills = await matchSkills(content, await context.stores.skills.list());
  const systemPrompt = buildSystemPrompt({ profile, skills: matchedSkills, evidenceRefs: refs });
```

(替换原来的 `buildSystemPrompt({ profile, evidenceRefs: refs })`)

- [ ] **Step 4: 运行确认通过**

Run: 同上命令(测试需用 `createMemoryStores([reviewSkill])` 注入技能,先跑通)
Expected: 全部用例 PASS。

- [ ] **Step 5: 提交**

```bash
git add -A && git commit -m "feat(api): executeTurn 技能按需注入"
```

---

### Task 5: GET /api/skills + 装配 + 收尾验收

**Files:**
- Modify: `packages/api/src/http/server.ts`(ApiDeps 加 skills + 路由)
- Modify: `packages/api/src/index.ts`(createSkillStore(config.skillsDir))
- Modify: `scripts/smoke.ts`(stores 加 skills)
- Modify: `packages/api/test/http-integration.test.ts`(追加用例)
- Modify: `README.md`

**Interfaces:**
- Produces: `GET /api/skills`

- [ ] **Step 1: 写失败测试(追加到 http-integration.test.ts)**

```ts
it('GET /api/skills 返回技能清单', async () => {
  const res = await fetch(`${baseUrl}/api/skills`);
  const skills = (await res.json()) as { id: string; name: string }[];
  expect(skills.length).toBeGreaterThan(0);
  expect(skills.map((s) => s.id)).toContain('tdd');
});
```

(集成测试 beforeAll 的 stores 需带技能:`createMemoryStores([{ id: 'tdd', name: '测试驱动开发', description: 'd', triggers: ['tdd'], prompt: '红绿重构' }])`)

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @meowbase/api exec vitest run test/http-integration.test.ts`
Expected: 新用例 FAIL(路由不存在)。

- [ ] **Step 3: 实现**

`packages/api/src/http/server.ts`:

```ts
// ApiDeps.stores 追加 skills: SkillStore
app.get('/api/skills', async () => deps.stores.skills.list());
```

`packages/api/src/index.ts`(stores 装配):

```ts
import { createSkillStore } from './stores/factories.js';
// stores 对象追加:
//   skills: createSkillStore(config.skillsDir),
```

`scripts/smoke.ts`(stores 追加):

```ts
import { createSkillStore } from '../packages/api/src/stores/factories.js';
// stores 对象追加:
//   skills: createSkillStore(join(process.cwd(), 'skills')),
```

`README.md`(追加):

```markdown
## Skills(M3)

消息中带触发词(如 "tdd"、"review"、"debug")时,对应技能会注入该轮上下文:

- `tdd` / `测试驱动` → 测试驱动开发
- `review` / `审查` / `代码评审` → 代码审查
- `debug` / `调试` / `bug` → 系统化调试
```

- [ ] **Step 4: 全量验证**

Run:

```bash
cd /Users/xuzishuo/code/meowbase && pnpm test && pnpm -r build && CLAUDE_BIN="$PWD/packages/api/test/fixtures/fake-claude.mjs" pnpm smoke
```

Expected: 全部用例 PASS(≥71),构建通过,冒烟通过。

- [ ] **Step 5: 提交**

```bash
git add -A && git commit -m "feat(api): GET /api/skills + 装配 + README"
```

---

## M3 完成后的验证清单

```bash
cd /Users/xuzishuo/code/meowbase
pnpm test
pnpm -r build
CLAUDE_BIN=.../fake-claude.mjs pnpm smoke
```

验收标准(来自 M3 spec §9):测试全绿且新增用例 ≥ 12;命中注入/不命中不注入由集成测试证明;fake 冒烟通过。
