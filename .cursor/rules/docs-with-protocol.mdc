---
description: 改协议或用户可见行为时，同一轮必须改文档
alwaysApply: true
---

# 文档跟代码一起走

协议、路由、球权、审批、记忆、拉闸词一变，同一轮改这些入口，不要留到「下次再说」：

- `AGENTS.md` 消息协议表和踩坑
- `README.md` 人怎么下任务 / 消息协议
- `docs/DEMO.md` 点击路径和口播（和演示不一致的句子删掉）
- `docs/A2A.md` 交棒 / 交接包 / 独立会话 / 公共证据（A2A 或记忆语义一变就改）
- `docs/features/` 对应功能稿的「怎么做 / 面试能讲」（语义一变就改）

**新功能先写设计再改代码**：复制 `docs/features/_template.md`，写清功能、价值、愿景、架构落点、为什么、怎么做、不做什么、面试能讲。对不上门或要新开邮箱 / SOP / MCP / 第二路由器，先标 `设计中` 或 `不做`。

旧计划稿（`docs/superpowers/plans/`）不改写历史。设计总稿若写了现行语义，同步一句。

对照：人能按文档点名、交棒、批准，不需要再问「句中 @ 还会不会叫猫」。