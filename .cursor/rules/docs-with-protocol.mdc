---
description: 改协议或用户可见行为时，同一轮必须改文档
alwaysApply: true
---

# 文档跟代码一起走

协议、路由、球权、审批、记忆、拉闸词一变，同一轮改权威源，不要留到「下次再说」。**不要在五处各写一遍完整规则。**

| 语义 | 唯一写全的地方 | 其余文档 |
|---|---|---|
| 消息协议（谁打什么、平台做什么） | `AGENTS.md` 协议表 | 引用，不复述规则 |
| 开发走到哪、每刀留下什么、下一刀往哪 | `docs/PROGRESS.md` | 引用。**产品现状**归 `README.md`，两者问的不是同一个问题 |
| 猫怎么协作（交接包、独立会话、沙箱证据） | `docs/A2A.md` | 引用 |
| 每一刀为什么这么切 | `docs/features/` 对应篇 | 引用 |
| 怎么跑起来、怎么配 | `README.md` | 点名留最小示例;其他能力**只点名有什么**,规则一律引用协议表 |
| 演示点击路径和口播 | `docs/DEMO.md` | 只写「点哪里、看到什么」，规则一律引用协议表 |

**一刀做完要在 `docs/PROGRESS.md` 记一条增量**：动了什么、与设计稿的偏离及原因、只有人手验过的部分、明确留了没做的。后两样别省——「全绿」对下一个接手的人没有信息量，「沙箱里 `git init` 会 `EPERM` 所以那个测试不真建仓」才有。增量**只往上加**，合并和删除只由主持这条线的人做，且必须先把沉淀搬进踩坑记录 / 功能稿 / 协议表再删。

**一次一个特性**：先写一篇薄设计（`docs/features/_template.md`），只做这一刀，做完再开下一篇。`设计中` 同时最多一篇。开篇先对照他们公开设计、写清能靠多近。不要在一篇里塞路线图，也不要一次写齐以后可能做的。

旧计划稿（`docs/superpowers/plans/`）不改写历史。设计总稿若写了现行语义，同步一句。

对照：人能按文档点名、交棒、批准，不需要再问「句中 @ 还会不会叫猫」。
