---
description: 制定计划前先看 clowder 怎么做，再按喵窝愿景和架构收窄
alwaysApply: true
---

# 计划先对照参考项目

参考项目是 [clowder-ai](https://github.com/zts212653/clowder-ai)（MIT）。喵窝是参照重写，不是 fork。

每次提出或修改计划（含「下一步做什么」）必须先查他们怎么处理同一问题：公开 README / TIPS / `docs/features` / issue，不读、不抄源码。

对照之后仍走喵窝自己的门：

1. **愿景**：平台只做路由、线程、身份、记忆、技能、审批、审计。人拍板，猫推理，平台当邮差。
2. **架构**：落在 `executeTurn`、名册配置、`shared` 纯函数、现有系统消息。不新开邮箱、SOP 引擎、MCP、队列、第二路由器。
3. **拿什么**：语义和踩坑（人和猫都只认行首 `@`、`@owner` 升级、句中提示不当球权、交接包给人看）。
4. **不拿什么**：实现、mailbox、hold_ball 工具、InvocationQueue、飞书/语音。

每个计划点仍说清：功能、价值、愿景、架构落点、给项目带来什么。对不上或要新开管线，就说不做。

一次只推进一个特性：先写一篇 `docs/features/`，做完再开下一篇。不要一次抛整条路线当「这一步」。

**节奏**：对照他们时看整条能力（例如异步 A2A 含交棒延后、邮箱、hold_ball），落地只做最薄的一刀。同一特性可以以后再加厚，不要第一刀就做成他们那么复杂。问一句答一句；人没点头不开下一刀。
