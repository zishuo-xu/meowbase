---
description: 制定计划前先看 clowder 怎么做，再按喵窝愿景和架构收窄
alwaysApply: true
---

# 计划先对照参考项目

参考项目是 [clowder-ai](https://github.com/zts212653/clowder-ai)（MIT）。喵窝是参照重写，不是 fork。

每次提出或修改计划、写 `docs/features/` 新篇，必须先想同一问题他们公开怎么设计、这一刀能靠多近：公开 README / TIPS / `docs/features` / issue。能靠就靠。不读、不抄源码。模板里「对照 / 靠拢」两句空着就还没想完。

对照之后仍走喵窝自己的门：

1. **愿景**：平台只做路由、线程、身份、记忆、技能、审批、审计。人拍板，猫推理，平台当邮差。
2. **设计尽量靠拢**：他们公开的架构图和能力语义是学习对象，能靠就靠。不是「另一套产品所以永远不做」。
3. **架构**：落在 `executeTurn` / `resumePendingTurn`、名册、`shared` 纯函数、现有系统消息。第一刀不新开第二心脏；加厚也尽量并进现有入口。
4. **拿什么**：公开设计、语义、踩坑。不读、不抄源码，不搬飞书/语音当默认下一刀。
5. **不拿什么**：他们的实现文件、InvocationQueue 原样、MCP 工具名。能力可以做成喵窝自己的薄片。

每个计划点仍说清：功能、价值、愿景、架构落点、给项目带来什么。对不上门再说不做。

一次只推进一个特性：先写一篇 `docs/features/`，做完再开下一篇。不要一次抛整条路线当「这一步」。

**节奏**：对照他们时看整条能力，落地只做最薄的一刀，以后加厚。问一句答一句；人没点头不开下一刀。
