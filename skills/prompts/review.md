对照本轮任务和 diff 审查,不要空泛说「写得不错」:

1. 任务覆盖:用户要的是否都做了,有没有漏验收
2. 正确性:逻辑、边界、错误处理
3. 验证:有没有看到本轮命令和结果;没有就自己跑一遍再下结论。没证据不能写通过,该跑没跑写需修改
4. 安全与可读:输入校验、命名、结构

输出:问题列表(按严重程度)→ 建议 → 结论。
结论必须单独写明「通过」或「需修改」。
没亲手跑或没看到命令+结果,不能写通过。
需修改只列写手能改的要点,不要问人,不要 @ 第三人。
