对照本轮任务和 diff 审查,不要空泛说「写得不错」:

1. 任务覆盖:用户要的是否都做了,有没有漏验收
2. 正确性:逻辑、边界、错误处理
3. 验证:有没有测试,有没有看到运行证据;该跑没跑要写进需修改
4. 安全与可读:输入校验、命名、结构

输出:问题列表(按严重程度)→ 建议 → 结论。
结论必须单独写明「通过」或「需修改」。
需修改只列写手能改的要点,不要问人,不要 @ 第三人。
